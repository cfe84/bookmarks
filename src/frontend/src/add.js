window.onload = () => {
    var app = new Vue({
        el: '#app',
        data: {
            currentFolder: {id: "root", name: "Home"}
        },
        methods: {
        }
    });
}

